{
  "name": "simple-slack-reporter",
  "version": "1.0.3",
  "description": "This is project for my reporters.",
  "author": "Marian Kucharski",
  "license": "ISC",
  "scripts": {
    "lint": "tslint --config tslint.json --project ./",
    "lint-fix": "tslint --config tslint.json --project ./ --fix",
    "tsc": "tsc"
  },
  "devDependencies": {
    "@slack/web-api": "^5.12.0",
    "request": "^2.88.2",
    "tslint": "^6.1.3"
  },
  "dependencies": {
    "typescript": "^4.0.2"
  }
}
